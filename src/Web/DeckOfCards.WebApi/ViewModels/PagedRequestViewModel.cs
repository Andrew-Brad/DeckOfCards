using System.ComponentModel.DataAnnotations;
using Microsoft.AspNetCore.Mvc;

namespace DeckOfCards.WebApi.ViewModels
{
    //public class PagedRequestViewModel // belongs in Middleware repo?  or MVC extensions?
    //{
    //    [Range(1, ulong.MaxValue, ErrorMessage = "Page number must be a valid positive integer.")]
    //    [FromQuery]
    //    public virtual ulong Page { get; set; } = 1;
    //    [Range(1, 1000, ErrorMessage = "Only 1000 widgets can be requested at once.")]
    //    [FromQuery]
    //    public virtual ulong ResultsPerPage { get; set; } = 50;
    //}
}
